// Espelha o CenterResponse do backend: id e a sigla (code). Dado de referência,
// usado para preencher o dropdown de centro no cadastro de curso.
export interface Center {
  id: number;
  code: string;
}
