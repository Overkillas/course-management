export const environment = {
  // URL base da API em desenvolvimento. Chamada direta ao backend; o CORS é
  // liberado no próprio Quarkus para localhost:4200 (decisão em docs).
  apiUrl: 'http://localhost:8080',
};
