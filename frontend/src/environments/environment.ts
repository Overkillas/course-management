export const environment = {
  // URL base da API REST consumida pelo frontend. Este é o arquivo de produção;
  // será revisitado no empacotamento (Etapa 7). Em desenvolvimento, vale o
  // environment.development.ts (substituído no build de dev pelo angular.json).
  apiUrl: 'http://localhost:8080',
};
